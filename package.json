{
  "name": "com.example.hello",
  "displayName": "HelloWorld",
  "version": "1.0.0",
  "description": "A sample Apache Cordova application that responds to the deviceready event.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "parcel build src/js/index.js --out-dir=www/dist --public-url=/dist",
    "watch": "parcel watch src/js/index.js --out-dir=www/dist --public-url=/dist",
    "dev": "npm run build && cordova serve"
  },
  "keywords": [
    "ecosystem:cordova"
  ],
  "author": "Apache Cordova Team",
  "license": "Apache-2.0",
  "devDependencies": {
    "cordova-browser": "^6.0.0",
    "cordova-ios": "^6.1.1",
    "cordova-osx": "file:../cordova-osx-AppleSilicon",
    "cordova-plugin-file": "^6.0.2",
    "cordova-plugin-whitelist": "^1.3.4",
    "cssnano": "^4.1.10",
    "prettier": "^2.1.2"
  },
  "peerDependencies": {
    "parcel": "1.12.4"
  },
  "cordova": {
    "plugins": {
      "cordova-plugin-whitelist": {},
      "cordova-plugin-file": {}
    },
    "platforms": [
      "browser",
      "ios",
      "osx"
    ]
  },
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  }
}